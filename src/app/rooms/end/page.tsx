

export default function endPage() {
    // returns the section of the page that appears on the end of the game

    return (
        <main>
            <div>
                
            </div>
        </main>
    );
}